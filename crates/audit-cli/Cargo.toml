[package]
name = "audit-cli"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
[dependencies]
audit-core = { path = "../audit-core" }
[lints.rust]
unsafe_code = "forbid"
